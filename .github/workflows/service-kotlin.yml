name: service-kotlin
on:
  push:
    paths:
      - service-kotlin/**
      - .github/service-kotlin.yml
  workflow_dispatch: {}
jobs:
  service-kotlin:
    strategy:
      matrix:
        build: [maven, gradle-groovy, gradle-kotlin]
        server: [micronaut]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-java@v3.1.0
        with:
          distribution: 'zulu'
          java-version: '11'

      - name: Get rendr
        run: |
          curl -L -H "X-JFrog-Art-Api:${{ secrets.JFROG_PASS }}" -O "https://specgen.jfrog.io/artifactory/binaries/rendr/latest/rendr_linux_amd64.zip"
          unzip -o rendr_linux_amd64.zip
          ./rendr help

      - name: Test service-kotlin
        run: |
          ./rendr render -set build=${{ matrix.build }} -set server=${{ matrix.server }} -set versions.specgen=2.1.1720 -noinput -out output file:///. service-kotlin
          cd output
          $(../rendr build .)